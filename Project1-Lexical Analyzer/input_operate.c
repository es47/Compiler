#include <stdio.h>

int main()
{
		int x, y, z;
		int array[100];
		for (x = 0; x <= 6; x++)
		{
				y--;
				x += x;
				x *= y;
				x /= x;
				x %= y;
				x -= y;
				z = x + y;
				z = x * y;
				z = x / y;
				z = x - y;
				z = x % y;
		}
		if (x == y)
				if (x & y)
						if (x | y)
								x &= y;
		if (x != y)
				if (x && y)
						if (x || y)
								x |= y;
		x >>;
		y <<;
		z ^= y;
		if (!x)
		{
				x = z ^ y;
				if (x >= y)
						x++;
				if (x < y)
						x--;
				if (x > y)
						y++;
		}
		z = max(x >= y) ? x: y;
		z -> b;
}
